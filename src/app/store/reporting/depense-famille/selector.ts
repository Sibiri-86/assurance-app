import {AppState} from '../../app.state';
export const depenseFamilleList = (state: AppState) => state.depenseFamilleState.depenseFamilleList;
export const selectByteFile = (state: AppState) => state.depenseFamilleState.reportFile;

