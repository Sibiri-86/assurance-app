import { TypePrime } from '../../parametrage/type-prime/model';
export interface Prime{
    typePrime?: TypePrime,
    primeFamille?: number,
    primeAdulte?: number,
    primeEnfant?: number,
    primeEmploye?: number,
    primeConjoint?: number
}